<div class="users_container border">
  <ul class = "users_list">
    <% @users.each do |user| %>
      <li>
        <a href="<%= user_path(user)%>">
          <%= render partial:'shared/user_profile', locals: { user: user} %>
        </a>
      </li>
    <% end %>
  </ul>
</div>